# Developer Report — P3-002

## Summary

- Date: 2026-08-06 Asia/Ho_Chi_Minh
- Task: P3-002 — Organization create/activate APIs
- Status: DONE
- Runtime Applied: YES
- Scope: minimal backend API only; no invitation accept lifecycle, tenant switch API, frontend, worker, or Phase 4 RBAC.

## Changed Files

- `apps/api/src/app.ts` — mounted `organizationRouter` at `/organizations`.
- `apps/api/src/modules/organizations/organization.routes.ts` — added `POST /organizations` and `POST /organizations/:organizationId/activate`.
- `apps/api/src/modules/organizations/organization.controller.ts` — added thin create/activate handlers.
- `apps/api/src/modules/organizations/organization.service.ts` — added bearer auth, active account check, create/activate orchestration, domain errors.
- `apps/api/src/modules/organizations/organization.repository.ts` — added transaction boundaries for create organization + creator membership and activate organization + membership.
- `apps/api/src/modules/organizations/organization.schemas.ts` — added Zod body/params validation.
- `apps/api/src/modules/organizations/organization.mapper.ts` — added safe DTO mapping for organization and membership.
- `apps/api/tests/tenancy/runtime-reconciliation-boundary.test.ts` — updated boundary assertion from pre-P3-002 no-route to P3-002 create/activate-only scope.
- `apps/api/prisma/schema.prisma` — restored missing `MembershipInvitation` model so Prisma schema validates against P3-001-A contract.

## Contract Evidence

- Create returns `disabled` organization and `inactive` creator membership.
- Activate transitions organization to `active`, sets `verifiedAt`, transitions creator membership to `active`, sets `activatedAt`.
- Duplicate slug maps to `409 ORGANIZATION_SLUG_EXISTS`.
- Missing/invalid bearer token maps to `401 AUTH_REQUIRED`.
- Invalid slug is rejected by Zod with `400 VALIDATION_ERROR`.
- DTOs do not expose token hashes, password hashes, refresh tokens, or session internals.

## Verification

- `npm run db:validate` — PASS
- `DATABASE_URL=postgresql://thesiflow:12345678@localhost:5433/thesiflow?schema=public npm run test --workspace apps/api -- tenancy/organization-create-activate.test.ts tenancy/organization-membership-reconciliation.test.ts tenancy/runtime-reconciliation-boundary.test.ts` — PASS, 3 files / 10 tests
- `npm run lint --workspace apps/api` — PASS
- `npm run typecheck --workspace apps/api` — PASS
- `npm run lint` — PASS, API/worker/web
- `npm run typecheck` — PASS, API/worker/web
- `DATABASE_URL=postgresql://thesiflow:12345678@localhost:5433/thesiflow?schema=public npm run test` — PASS, 18 files / 52 tests
- `npm run build` — PASS, API/worker/web

## Notes

- No database migration was added for P3-002.
- Prisma schema validation initially exposed a missing tracked `MembershipInvitation` model from P3-001-A; restored it and regenerated Prisma client before final checks.
- DB tests still need unsandboxed execution because sandbox blocks Prisma adapter PostgreSQL access.
- No commit or push performed.

## Next Exact Action

Create/read P3-003 task and implement membership invitation/accept lifecycle; do not start P3-004 or Phase 4.
