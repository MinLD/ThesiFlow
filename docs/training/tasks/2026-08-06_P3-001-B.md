# Daily Development Task — P3-001-B

## 1. Thông tin chung

- Ngày: 2026-08-06
- Phase: Phase 3 — Organization/Tenant onboarding
- Parent task: P3-001 — Organization/membership model reconciliation
- Task ID: P3-001-B
- Trạng thái: TODO
- Độ khó: Low
- Thời lượng dự kiến: 1–2 giờ
- Dependency: P3-001-A PASS_WITH_MINOR_NOTES, commit `c54c2b9`
- Blocker: Không có
- Database mode: DATABASE_SPEC_ONLY — no new database change
- Test owner: AI
- Implementation owner: Developer

## 2. Mục tiêu trong ngày

Xác nhận sau khi DB đã chuyển sang `Organization`/`TenantMembership`/`MembershipInvitation`, runtime source không còn phụ thuộc legacy `Tenant` access và chưa mở organization API trước task P3-002.

## 3. Kết quả cuối ngày phải có

- Không có runtime legacy `prisma.tenant`, `tenantId`, `tenant_id` trong API source/seed được kiểm tra.
- Không có `/organizations` hoặc `/organization` route trong `apps/api/src/app.ts` trước P3-002.
- Test AI cung cấp pass.
- Developer report tồn tại tại `docs/training/reports/2026-08-06_P3-001-B_REPORT.md`.

## 4. Kiến thức developer cần luyện

- Runtime boundary review sau database migration.
- Scope control giữa DB reconciliation và API implementation.
- Static regression test cho architecture boundary.
- Không nhảy task khi phase chưa qua gate.

## 5. Hiện trạng repository

- P3-001-A đã commit `c54c2b9`.
- Prisma schema/migration/seed đã dùng `Organization`, `TenantMembership`, `MembershipInvitation`.
- Auth/runtime source hiện chưa có organization module, tenant context middleware hoặc organization API.
- `apps/api/src/app.ts` chỉ mount `/auth`, `/health`, `/ready`, `/api/v1/meta`.
- Không phát hiện legacy tenant runtime access trong các file kiểm tra của P3-001-B.

## 6. Database contract

Không có thay đổi database trong task này. Contract P3-001-A giữ nguyên.

## 7. Database code

Không áp dụng.

## 8. Giải thích database design

Không thuộc phạm vi task này. Database design đã chốt ở `docs/training/database-specs/2026-08-06_P3-001-A_DATABASE.md`.

## 9. Backend implementation developer phải làm

Không viết runtime implementation nếu test đang pass.

Developer chỉ cần:

- Chạy test boundary do AI tạo.
- Kiểm tra thủ công `apps/api/src/app.ts`, `apps/api/src/modules/auth/*`, `apps/api/prisma/seed.ts` không còn legacy tenant runtime access.
- Nếu test fail, chỉ sửa đúng legacy reference hoặc premature route nằm trong scope.
- Không tạo controller/service/repository organization.
- Không tạo tenant context switch.

## 10. API contract

Không có API mới. Organization create/activate API thuộc P3-002.

## 11. Frontend requirements

Không thuộc phạm vi task này.

## 12. Automated test do AI viết

Test file: `apps/api/tests/tenancy/runtime-reconciliation-boundary.test.ts`.

Test cases:

1. Runtime source/seed không dùng legacy `prisma.tenant`, `tenantId`, `tenant_id`.
2. `app.ts` không expose organization route trước P3-002.

## 13. Trạng thái test ban đầu

| Test | Trạng thái ban đầu | Lý do |
|---|---|---|
| Legacy tenant runtime access absent | PASS | P3-001-A đã reconcile seed/source cần thiết |
| No premature organization API route | PASS | P3-002 chưa bắt đầu |

Command đã chạy: `npm run test --workspace apps/api -- tenancy/runtime-reconciliation-boundary.test.ts` — PASS 1 file / 2 tests.

## 14. Phạm vi developer phải làm

- Không sửa runtime nếu target test pass.
- Chạy command trong task.
- Nộp report ngắn xác nhận không cần runtime/domain reconciliation thêm trước P3-002, hoặc ghi rõ finding nếu phát hiện gap.

## 15. Ngoài phạm vi

- Không làm P3-002 organization create/activate APIs.
- Không làm P3-003 invitation lifecycle.
- Không làm P3-004 tenant context switch.
- Không làm frontend.
- Không thêm DB migration.
- Không refactor auth module.

## 16. Business invariants

| Invariant | Protection | Test |
|---|---|---|
| Account không phụ thuộc legacy tenant runtime | Static boundary scan | Legacy tenant runtime access absent |
| P3-002 không bị implement sớm | Route boundary scan | No premature organization API route |

## 17. File dự kiến developer thay đổi

Không có nếu test pass.

Nếu test fail ngoài dự kiến:

- `apps/api/src/app.ts`
- `apps/api/src/modules/auth/*`
- `apps/api/prisma/seed.ts`

## 18. File AI đã tạo hoặc cập nhật

- `apps/api/tests/tenancy/runtime-reconciliation-boundary.test.ts`
- `docs/training/tasks/2026-08-06_P3-001-B.md`
- `docs/ROADMAP.md`
- `docs/phases/phase-3/PHASE_3_PLAN.md`
- `docs/phases/phase-3/PHASE_3_CODE.md`

## 19. Acceptance criteria

- [ ] Target boundary test pass.
- [ ] No runtime implementation added unnecessarily.
- [ ] No organization API route before P3-002.
- [ ] No legacy tenant runtime access in checked files.
- [ ] Developer report exists.
- [ ] Existing P3-001-A test remains pass.

## 20. Command developer phải chạy

```bash
npm run test --workspace apps/api -- tenancy/runtime-reconciliation-boundary.test.ts
npm run test --workspace apps/api -- tenancy/organization-membership-reconciliation.test.ts
npm run lint --workspace apps/api
npm run typecheck --workspace apps/api
```

## 21. Câu hỏi developer phải trả lời

- Vì sao không nên tạo `/organizations` route trong P3-001-B?
- Nếu còn `tenantId` trong auth source sau P3-001-A thì rủi ro là gì?
- P3-002 cần bắt đầu từ test/API contract nào?

## 22. Báo cáo developer phải nộp

Developer tạo `docs/training/reports/2026-08-06_P3-001-B_REPORT.md`.
